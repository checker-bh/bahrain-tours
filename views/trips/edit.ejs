<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Editing <%= trip.title %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit <%= trip.title %> at <%= trip.NAME %></h1>
    <form 
      action="/users/<%= user._id %>/trips/<%= trip._id %>?_method=PUT"
      method="POST"
    >
      <label for="NAME">NAME:</label>
      <input 
        type="text" 
        name="NAME" 
        id="NAME" 
        value="<%= trip.NAME %>"
      >
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" value="<%= trip.title %>">
      <label for="notes">Notes:</label>
      <textarea name="notes" id="notes"><%= trip.notes %></textarea>
      <label for="postingLink">Posting Link:</label>
      <input 
        type="text" 
        name="postingLink" 
        id="postingLink" 
        value="<%= trip.postingLink %>"
      >
      <label for="status">Status:</label>
      <select id="status" name="status">
        <option 
          value="Bahrain" 
          <%= trip.status === "Bahrain" ? "selected" : "" %>
        >
        Bahrain
        </option>
        <option 
          value="Kuwait" 
          <%= trip.status === "Kuwait" ? "selected" : "" %>
        >
        Kuwait
        </option>
        <option 
          value="Oman" 
          <%= trip.status === "Oman" ? "selected" : "" %> 
        >
        Oman
        </option>
      
    
      </select>
      <button type="submit">Update trip</button>
    </form>
</body>
</html>